resource "random_pet" "server" {
    length = var.pet_length
    prefix = var.pet_prefix
}

resource "random_pet" "server_2" {
    length = var.pet_length
    prefix = var.pet_prefix
}

resource "random_pet" "server_3" {
    length = var.pet_length
    prefix = var.pet_prefix
}

resource "random_pet" "server_4" {
    length = var.pet_length
    prefix = var.pet_prefix
}

resource "random_pet" "server_5" {
    length = var.pet_length
    prefix = var.pet_prefix
}